version: "3.7"

services:
  test-mysql:
    image: hypriot/rpi-mysql
    platform: linux/amd64
    environment:
      MYSQL_ROOT_HOST: '%'
      MYSQL_DATABASE: 'test'
      MYSQL_ALLOW_EMPTY_PASSWORD: 'yes'
      TZ: Asia/Seoul
    ports:
      - "3306"
